# ><%= "[ #{name} ]".center(75,'-') %><

@current_recipe = "<%= key %>"
@before_configs["<%= key %>"].call if @before_configs["<%= key %>"]
say_recipe '<%= name %>'

<%= config.compile if config %>
@configs[@current_recipe] = config

<%= template %>
